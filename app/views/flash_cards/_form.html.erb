<%= form_with(model: flash_card) do |form| %>
  <% if flash_card.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(flash_card.errors.count, "error") %> prohibited this flash_card from being saved:</h2>

      <ul>
        <% flash_card.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :word, style: "display: block" %>
    <%= form.text_field :word %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
